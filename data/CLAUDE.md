# data/ — Claude context

User-facing notes are in [`README.md`](README.md); this file is for AI assistants continuing the work.

## What's safe to commit here

- `keys.txt` — pure list of integers, no copyrighted content. ✅
- `keys-<previous-version>.txt` — per-version frozen snapshots of `keys.txt`, kept for cross-version reference and so users with saved CE tables can still find the right index ordering for older patches. Same integer-only content; same safe-to-commit rationale.
- `gamedata-keys-<version>/<table>.txt` — per-table key dumps for every non-iteminfo gamedata table the c_abi bridges read (skill, mission, quest, stage, gimmick, character, faction, store, mercenary, dye triple, niche bridges, etc.). One decimal `uint32` per line, in PABGH appearance order (= in-game iteration order). Plays the same role `keys.txt` does for iteminfo: a per-version snapshot for cross-version diffing. Regenerated by [`../scripts/dump_gamedata_keys.py`](../scripts/dump_gamedata_keys.py). Integer-only — safe.
- `internal_name_overrides.json` (hypothetical, not yet present) — community-curated friendly names for dev items. Could be added to humanise the `string_key` fallback in `scripts/export_for_ce.py`.

## What is NOT safe to commit here (or anywhere)

Anything derived from `iteminfo.pabgb`, `*.paloc`, or other paz-archive contents. These are Pearl Abyss assets. Examples:
- `items.jsonl` — even though it's just JSON, it contains Korean item descriptions and English item names from paloc.
- `paloc_*.json` — translation tables.
- `iteminfo_*.pabgb` — raw game binaries.
- Any `output*.txt` derived from paloc lookups.
- `v1.0?-mem*.CEM` — raw process-memory dumps. Even if used only for RE diagnostics, dropping them in the repo would leak game-side memory layout. Keep these on disk under `data/` for current-session use, but never `git add` them; `.gitignore` already covers `*.CEM`.

Such files belong under `out/` (gitignored) or `references/samples/` (gitignored).

## When `keys.txt` needs replacing

After every game patch that adds/removes items. The patch notes will tell you "X new items added"; if the count or values in `keys.txt` haven't changed, the existing file is still valid.

If parser fixes for a new patch require regenerating, snapshot the previous `keys.txt` first as `keys-<old-version>.txt` next to the new one — the *order* matters because Cheat Engine table dropdowns reference items by index. A saved table from a prior version will desync if `keys.txt` ordering changes between game patches.

## In-memory array layout (relevant when re-RE'ing the dumper)

The game keeps the itemKey table in process memory as a packed `[u32 itemKey][u32 offset]` array — the second u32 is the byte offset of that item inside `iteminfo.pabgb`. This was confirmed in the 1.06 RE by dumping the array region: `offset` grows monotonically by ~600 B per slot, and the last live slot's `offset` matches the anchor scan's offset for the final item exactly. Two practical consequences:

- The CE Lua dumper terminates on `offset` monotonicity break — this is what cleanly delimits the array end. Sentinel checks (`key == 0xFFFFFFFF` / `key == 0`) are defensive backstops, not the primary signal.
- Anchor offsets produced by `scripts/export_for_ce.py` should match the `offset` field of the same key in the live-memory array. A future cross-check (`--verify-with-mem-dump`) could exploit this as a smoke test that the anchor scanner hasn't drifted.

## Past versions

| Patch | `keys.txt` shape | Where the snapshot lives now |
|---|---|---|
| 1.04 | Not preserved — older CE-table flow read `items.jsonl` directly | — |
| 1.05 | 6,236 keys | [`keys-1.05.01.txt`](keys-1.05.01.txt) (frozen 2026-05) |
| 1.06 | 6,253 keys | [`keys-1.06.txt`](keys-1.06.txt) (byte-identical to `keys-1.07.txt`) |
| 1.07 | 6,253 keys | [`keys-1.07.txt`](keys-1.07.txt) — 1.06 and 1.07 ship identical key lists |
| 1.08 | 6,314 keys | [`keys-1.08.txt`](keys-1.08.txt) snapshot. +61 keys vs 1.07. |
| 1.09 | 6,314 keys | [`keys-1.09.txt`](keys-1.09.txt) — byte-identical to `keys-1.08.txt` (content-only patch, no item add/remove). |
| 1.10 | 6,325 keys | [`keys-1.10.txt`](keys-1.10.txt) snapshot. +11 keys vs 1.09. 1.10 also ships two iteminfo schema drifts — see `src/item_info/item.rs`. |
| 1.11 | 6,333 keys | [`keys-1.11.txt`](keys-1.11.txt) snapshot. +8 keys vs 1.10. 1.11 ships one iteminfo schema drift (new boolean `u8` between `apply_drop_stat_type` and `drop_default_data`) and **no** save-body drift — see `src/item_info/item.rs`. |
| 1.12 | 6,483 keys | [`keys-1.12.txt`](keys-1.12.txt) snapshot. +150 keys vs 1.11. 1.12 ships **four** iteminfo schema drifts (SubItem `type_id==16`, `unk_pre_max_endurance`, equipment/gem-gated `unk_pre_gimmick_visual`, `EnchantData` inter-element separators) and **no** save-body drift — see `src/item_info/item.rs`. |
| 1.13 | 6,508 keys | [`keys-1.13.txt`](keys-1.13.txt) snapshot. +25 keys vs 1.12 (+29 / −4). Largest structural iteminfo change yet — prefab/gimmick-visual lists merged and relocated to the item end. |
| 1.14 | 6,508 keys | [`keys-1.14.txt`](keys-1.14.txt) — byte-identical to `keys-1.13.txt` (content-only patch, no item add/remove). |
| 1.15 | 6,508 keys | [`keys-1.15.txt`](keys-1.15.txt) — byte-identical to `keys-1.14.txt` (content-only patch). |
| 1.16 | 6,581 keys | [`keys-1.16.txt`](keys-1.16.txt) snapshot. +73 keys vs 1.15, none removed. 1.16 ships four iteminfo schema drifts **and** the first-ever skill drift — see `src/item_info/item.rs`. |
| 1.17 | 6,572 keys | [`keys-1.17.txt`](keys-1.17.txt) snapshot. **−9 keys vs 1.16, none added**: 1004912–1004920, the nine `Item_Set_*_Tier0_Reminiscence` entries. Content-only — no schema drift anywhere. |
| 1.18 | 6,573 keys | [`keys-1.18.txt`](keys-1.18.txt) snapshot. +1 key vs 1.17 (1005446 `Demian_Greyfur_Fabric_Cloak_II`), none removed. One iteminfo schema drift (`MergedPrefabVisualData.unk_pre_is_craft_material`) — see `src/item_info/item.rs`. |
| **2.00** | **6,810 keys** | [`keys.txt`](keys.txt) (current) + [`keys-2.00.txt`](keys-2.00.txt) snapshot. **+237 keys vs 1.18, none removed.** The first *major* version bump (paver `2 / 0 / 0`). Two iteminfo schema drifts (`SubItem` tag 18; `unk_pre_max_endurance_a`) — see `src/item_info/item.rs`. |

The other 30 gamedata tables get the same treatment under `gamedata-keys-1.08/` … [`gamedata-keys-2.00/`](gamedata-keys-2.00/) — per-version snapshots so the next patch's drift can be diffed table-by-table without needing the previous game install on disk. **Diff the key *sets*, not the file hashes**: the dumper writes LF, but committed snapshots check out as CRLF under `core.autocrlf=true`, so a hash comparison flags all 30 tables as changed even when every key is identical. 2.00 deltas vs 1.18 (30 tables / 96,997 keys): knowledgeinfo +326, stageinfo +265, gimmickinfo +112, missioninfo +37, characterinfo +25, skill +19, gameadviceinfo +6, gameplayvariableinfo +3, globalgameevent +3, questgaugeinfo +2, questinfo +2, partprefabdyeslotinfo +1, storeinfo −1; all 17 other tables key-identical. (The globalgameevent +3 is a substitution, not growth: `RoyalSupply` 0x424a was removed and four per-faction `RoyalSupply_*` rows 0x4308–0x430b added.) 1.18 deltas vs 1.17: partprefabdyeslotinfo +65, gimmickinfo +35, skill +14, knowledgeinfo +10, houseinfo +8, storeinfo +5, itemgroupinfo +1, triggerregioninfo +1, characterinfo −18; all 21 other tables key-identical. 1.17 deltas vs 1.16: gimmickinfo +1 (new key 1012695), itemgroupinfo −1 (removed key 18566); all 28 other tables key-identical. 1.16 deltas vs 1.15: gimmickinfo +228, stageinfo +153, knowledgeinfo +148, characterinfo +139, storeinfo +139, itemgroupinfo +47, partprefabdyeslotinfo +16, skill +14, missioninfo +4, questinfo +4, mercenaryinfo +2, gameplayvariableinfo −1, reserveslot −2. 1.15 vs 1.14: zero key changes in any table. 1.14 vs 1.13: knowledgeinfo −1. 1.13 deltas vs 1.12: partprefabdyeslotinfo +570, characterinfo +16, gimmickinfo +15, itemgroupinfo +9, knowledgeinfo +8, skill +6, questinfo +4, mercenaryinfo/gameplayvariableinfo/missioninfo/stageinfo +1 each, storeinfo −53, reserveslot −1. 1.12 deltas vs 1.11 (diff the dirs): gimmickinfo +91, knowledgeinfo +72, storeinfo +53, stageinfo +37, itemgroupinfo +20, characterinfo +7, skill +5, reserveslot +4, crafttoolinfo/crafttoolgroupinfo/gameadviceinfo/questinfo +2 each, gameplayvariableinfo +1, partprefabdyeslotinfo −143, factionnode −22; all 15 other tables unchanged. 1.11 deltas vs 1.10: gameplayvariableinfo +8, missioninfo +9, stageinfo +9, questinfo +1; all 26 other tables unchanged. 1.10 deltas vs 1.09: missioninfo +134, stageinfo +358, gimmickinfo +282, questinfo +65, knowledgeinfo +43, questgaugeinfo +43, itemgroupinfo +14, characterinfo/skill/factionspawndatainfo +8, gameadviceinfo +7, partprefabdyeslotinfo +6, regioninfo/storeinfo +1, factionnode −17.
