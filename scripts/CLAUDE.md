# scripts/ — Claude context

Engineering notes for the next session. User-facing docs live in [`README.md`](README.md). Full RE history (the long version) is in [`../docs/archive/1.05-parser-history.md`](../docs/archive/1.05-parser-history.md).

## Status

- **ItemInfo parser**: byte-perfect on **1.05** (6,236 items), **1.06 / 1.07** (6,253 items — 1.07 ships an identical key list to 1.06), **1.08** (6,314 items — +61 vs 1.07; 1.08 ships three schema drifts vs 1.07, see `src/item_info/item.rs` header), **1.09** (6,314 items — key list byte-identical to 1.08, no schema drift) **1.10** (6,325 items — +11 vs 1.09; **two schema drifts**: removed `money_icon_path` u32, added `UnitData.unk_post_icon_path` u32 — see `src/item_info/item.rs` header) **1.11** (6,333 items — +8 vs 1.10; **one schema drift**: a new boolean `u8` (`unk_post_apply_drop_stat_type`) inserted between `apply_drop_stat_type` and `drop_default_data` — see `src/item_info/item.rs` header) **1.12** (6,483 items — +150 vs 1.11; **four schema drifts**: SubItem `type_id==16` payload-free variant, unconditional `unk_pre_max_endurance: u32`, equipment/gem-gated `unk_pre_gimmick_visual: u32`, and `enchant_data_list` N-1 inter-element u32 separators via the new `EnchantDataList` type — see `src/item_info/item.rs` header; the conditional gimmick field drove a `=> <cond>` extension of `py_binary_struct!`) and **1.13** (6,508 items — +25 vs 1.12; **structural relocation**: SubItem `type_id==17`, and `prefab_data_list` + `gimmick_visual_prefab_data_list` **merged into one list relocated to the item end** (`MergedPrefabVisualData`), plus a constant `0xff00` item tail — `enchant_data_list` + `unk_pre_gimmick_visual` stay put; see `src/item_info/item.rs` header) and **1.14** (6,508 items — content-only over 1.13; iteminfo layout byte-identical, no schema drift in any subsystem) and **1.15** (6,508 items — content-only over 1.14; iteminfo layout byte-identical, item **values** changed, no schema drift and the 30-table gamedata key set byte-identical to 1.14) and **1.16** (6,581 items — +73 vs 1.15; **four schema drifts**: head-side `inventory_info` removed, `DockingChildData.unk_post_summon_tag` removed (conditional), a `u32 + u8 + CArray<UnkPreRespawnData> + u8` block inserted before `respawn_time_seconds` with `unk_pre_max_endurance` **swapped ahead of it**, and `inventory_info_list: [u16; 9]` at the item end absorbing the old constant `unk_tail` — see `src/item_info/item.rs` header) and **1.17** (6,572 items — **−9** vs 1.16, the `Item_Set_*_Tier0_Reminiscence` block; content-only, iteminfo layout byte-identical, zero size-changed items) and **1.18** (6,573 items — +1 vs 1.17; **one schema drift**: a new `u32` (`unk_pre_is_craft_material`, constant `0xeac5e173`) in every `MergedPrefabVisualData` element, between `tribe_gender_list` and the 3-byte flag tail) and **2.00** (6,810 items — +237 vs 1.18, none removed; **two schema drifts**: `SubItem` gained payload-free `type_id == 18`, and a new always-zero `u32` (`unk_pre_max_endurance_a`) sits directly ahead of `unk_pre_max_endurance` — see `src/item_info/item.rs` header). `serialize_iteminfo` roundtrips every item; the pipeline (`scripts\export_for_ce.py`) runs end-to-end clean on every version.
- **Skill parser** (`src/skill_info/`): byte-perfect roundtrip on 1.03 / 1.04 / 1.05; the `c_abi_skillinfo_live_roundtrip` test runs against the live install and is green on 1.07 (so 1.06 / 1.07 are covered too, even without a dedicated re-probe). The brute-force BuffData subclass-tail probe absorbs size drift, so unless the format flag flips, no change is expected. The brute-force probe is essential — cross-version probing in May 2026 showed 11 `type_id` sizes drift between 1.03–1.05, so the size table cannot be hardcoded. **1.16 shipped the first drift the probe could NOT absorb**: it landed in `PostBuff` (a new `u8` before `damage_type`), which the probe does not cover, and broke 589 of 2,013 entries. Use `_probe_skill_entry_failures` (see below) to tell "one odd new skill" from systematic drift before starting any RE. **1.17 left `skill.pabgb`/`.pabgh` byte-identical to 1.16** — before RE'ing any suspected skill drift, hash the two versions' `skill.pabgb` in the portable archive; if they match, the parser cannot have regressed and any probe failure is an artifact. **1.18 changed `skill.pabgb` (+4,538 B, 2,013 → 2,027 entries) with zero drift** — the probe reported 2027/2027 ok, so a changed hash is not by itself evidence of drift either; run the probe before assuming anything. **2.00 likewise: +23,017 B, 2,027 → 2,046 entries, probe 2046/2046 ok, format still `WithField58`** — even a first-ever *major* version bump left the skill parser untouched. **2.01 likewise: 2,046 → 2,061 entries, probe 2061/2061 ok** — but note 2.01 renamed the files themselves (`skill.pabgb`/`.pabgh` → `skill.staticinfobody`/`.staticinfoheader`), so a "file not found" on a fresh patch is the container moving, not the parser breaking.
- **Archive layout**: Pearl Abyss has moved the gamedata tables once, in **2.01** — `0008/gamedata/binary__/client/bin/<t>.pabgb`/`.pabgh` → `0008/gamedata/binarystaticinfo__/bin/<t>.staticinfobody`/`.staticinfoheader`, and each language's `localizationstring_<lang>.paloc` → 39 per-namespace files under `gamedata/stringtable/binary__/<lang>/`. File *contents* were untouched. Nothing in the library hardcodes these; the resolvers are [`gamedata_layout.py`](gamedata_layout.py) (scripts) and `src/binary/gamedata_layout.rs` (`#[cfg(test)]`, for the live-install tests). Both try the newest layout first and fall back, so an older install still reads — add a new entry at the front of `BIN_LAYOUTS` / `LAYOUTS` if it moves again.
- **CI gate** (`.github/workflows/ci.yml`): every push to `main`/`dev` and every PR runs `cargo clippy --all-targets --lib -- -D warnings` + `cargo test --lib` on Ubuntu. Branch protection on `main` requires the check before merge.

The most likely trigger for the next session of work is a new game patch — see "On a new game patch" below.

## On a new game patch

The fastest "did Pearl Abyss break anything?" loop:

0. **Snapshot the new version's gamedata bin FIRST — before anything else touches it.**

    ```powershell
    $env:CRIMSON_GAMEDATA_BIN = "X:\Crimson Desert\gamedata-bin"   # wherever the archive lives
    python scripts\extract_gamedata_bin.py --version <new>
    ```

    Then append a `<new>` entry to that folder's `README.txt`. This is the
    step that makes every later question answerable, because the previous
    version's install is gone the moment Steam patches. **On 2.01 this got
    skipped and the RE went down a wrong path**: the `partprefabdyeslotinfo`
    mask widening was argued from value distributions inside the *new* file
    alone, concluding the historical 3 bytes had been kept and 9 appended.
    A tandem walk against the kept 2.00 binary — 30 seconds once the archive
    is there — showed the field had been re-encoded outright and no sub-slice
    was the old mask, which also changed the C ABI story. Reach for the
    archive before arguing from statistics; distributions describe a file,
    a diff explains it.

    Note the archive holds `0008`'s tables only. PALOC is deliberately
    excluded (content, ~242 MB/version), so localization-text questions —
    "did this quest title change?" — are **not** answerable retroactively.
    If a patch's text matters, extract the paloc you care about too.

1. Update the live game install. Refresh `data\keys.txt` via the CE Lua dumper (see [`../data/README.md`](../data/README.md)). Run `python scripts\export_for_ce.py`. If parser status comes back `ok=<N>  leftover=0  fail=0  no_anchor=0`, no schema drift — you're done; that `N` is the new item count.

    If it instead dies with `directory '...' not found in 0008/0.pamt` — the way 2.01 did — the container moved, not the schema. Dump the new tree with

    ```python
    import crimson_rs
    p = crimson_rs.parse_pamt_file(r"<game>\0008\0.pamt")
    for d in p["directories"]:
        print(d["file_count"], d["path"])
    ```

    and add the new `(dir, body_ext, header_ext)` triple to the front of `BIN_LAYOUTS` in [`gamedata_layout.py`](gamedata_layout.py) and `LAYOUTS` in `src/binary/gamedata_layout.rs` (plus `PALOC_ROOT` handling if localization moved too). Everything downstream picks it up.
2. Optional but cheap: run `cargo test --lib`. The iteminfo / skill roundtrip tests are pinned to the local game install and skip cleanly when the install isn't on the expected version, so a green run gives extra confidence (a red run is informational, not blocking).
3. If iteminfo regressed (`fail > 0` or `leftover > 0`), follow "Investigation order" below.
4. If skill regressed, **first run the per-entry diagnostic** — `SkillData::parse` aborts on the first bad entry, which cannot distinguish one odd new skill from systematic drift:

    ```powershell
    cargo test --lib _probe_skill_entry_failures -- --ignored --nocapture
    ```

    It prints `skill entries: N total, X ok, Y failed` plus the first 40 failures. (Backed by `skill_info::probe_entry_failures`. Note the probe can hit a huge bogus allocation on badly-misaligned entries and abort the process — just re-run; it is non-deterministic via the `TailSizeCache` hash order. **The same artifact can take down the whole `cargo test --lib` process**, not just the probe: on 1.18 two consecutive default-threaded runs ballooned past 13 GB and died with `STATUS_STACK_BUFFER_OVERRUN` after ~200 tests, then `-- --test-threads=4` ran green twice. Reach for that flag before suspecting a real regression.) A high failure count with *old* keys among them means schema drift; a single failing brand-new key means content. For 1.16 this reported 589/2013 and pointed straight at `PostBuff`, which the BuffData tail probe does not cover.

    Then drop a copy of the new `0008/` archive next to the previous baselines (under `BASELINES_ROOT`) and run [`archive/probe_skill_versions.py`](archive/probe_skill_versions.py) with the new version added to its `VERSIONS` list. The drift table at the bottom shows exactly which `type_id` tail sizes changed (and whether the format flag flipped). Update `src/skill_info/` accordingly — usually nothing needs changing because the brute-force probe absorbs size drift, but a new format-flag flip would need parser logic changes.
5. **Save-side probes** (run only if downstream reports the editor breaking; not part of the routine green-light loop). Five `#[ignore]` diagnostics live in [`../src/c_abi/character_info.rs`](../src/c_abi/character_info.rs) and skip cleanly when their inputs are missing:
    - `_scan_all_groups_for_portrait_like_names` (in [`../src/c_abi/paz.rs`](../src/c_abi/paz.rs)) — buckets every portrait-like filename in the install by prefix. Catches Pearl Abyss reorganising NPC asset paths.
    - `_probe_live_save_field_blocks` — walks `FieldNPCSaveData` (228) and `FieldGimmickSaveData` (4264) blocks in a live save, dumps their field-level schemas and cat-byte distributions. Catches save-block schema drift.
    - `_scan_0008_appearance_files` — lists every `*appearance*` file in `0008/0.pamt`. Catches Pearl Abyss renaming the appearance tables.
    - `_probe_character_appearance_index` — validates the pinned save→pabgh transform for `CharacterAppearanceIndexKey` against the live save + game install. See [`../docs/save-editor-keys-plan.md`](../docs/save-editor-keys-plan.md) §9 for the resume plan.
    - `_probe_abyss_gate_mapping` — builds the per-gate abyss-gate mapping (gimmickinfo + save FieldGimmickSaveData) and pins the three known `_initStateNameHash` constants the Path B editor (`CrimsonAtomtic`) uses for per-gate unlock controls. Writes `out/abyss_gate_probe/mapping.json` (gitignored). See [`../docs/abyss-gate-map.md`](../docs/abyss-gate-map.md) for the full picture.
    - `_probe_paloc_template_density` — per-namespace count of template-bearing PALOC entries (`{StaticInfo:Type:Key#label}`, `<br/>`, `%0`/`%1`, `[EMPTY]` sentinels). Source of the "do we need a template-resolver?" answer in [`../docs/paloc-template-survey.md`](../docs/paloc-template-survey.md). Run when expanding bridge scope beyond titles into descriptions / dialogue.
    - `_probe_inventory_save_data_schema` — dumps `InventorySaveData → _inventorylist[N] → _itemList[M]` field tree. Source-of-truth for [`crimson_save_list_inventory_items`](../src/c_abi/mod.rs); re-run after a patch if `list_inventory_items` starts mis-parsing.
    - `_probe_item_dye_data` — walks `ItemSaveData._itemDyeDataList` across the save, dumps the per-element `ItemDyeSaveData` field schema (9 fields including the PyQt5 RE-missed `_disableSymbol`) + scans 0008 for `dye*.pabgb` gamedata. Source data for [`../docs/dye-editor-scope.md`](../docs/dye-editor-scope.md).
    - `_probe_dye_gamedata_tables` + `_probe_dye_gamedata_rows` — phase-1/2 RE of the three `dye*.pabgb` tables. Dumps row-by-row schema; used as the source for [`../docs/dye-editor-scope.md`](../docs/dye-editor-scope.md)'s "Verified row schemas" tables and the bridges under `src/{dye_color_group_info,part_prefab_dye_texture_pallete_info,part_prefab_dye_slot_info}/`.
    - `_probe_item_socket_data` + `_probe_item_socket_data_anywhere` + `_probe_item_socket_data_all_slots` + `_probe_item_socket_data_full_scan` — four-phase RE of the `_socketSaveDataList` schema (slot104-focused → anywhere-in-tree → cross-save histogram → anomaly hunt). Source data for [`../docs/socket-editor-scope.md`](../docs/socket-editor-scope.md).
    - `_probe_artifact_challenge_mapping` (in [`../src/c_abi/iteminfo.rs`](../src/c_abi/iteminfo.rs)) — verifies the 1:1 invariant between `iteminfo.look_detail_mission_info` and `Challenge_SealedArtifact_*` missions. Source data for [`../docs/artifact-challenge-mapping.md`](../docs/artifact-challenge-mapping.md). Re-run after a patch to confirm the invariant still holds.
    - `_probe_iteminfo_gem_classification` (in [`../src/c_abi/iteminfo.rs`](../src/c_abi/iteminfo.rs)) — finds the iteminfo field combo (`item_type=74` + `category_info=2501`) that marks an itemkey as a gem. Source of the `crimson_iteminfo_canonical_gem_*` ABI.
    - `_scan_0008_faction_files` + `_probe_faction_pabgh_shapes` + `_probe_faction_small_tables` + `_probe_faction_paloc_chains` — four-phase RE of the faction-bridge gamedata (`factionnode.pabgb`, `factionspawndatainfo.pabgb`, `factionrelationgroup.pabgb`, plus the bonus `factiongroup` / `factionnodespawninfo` / `factionwaypoint` / `allygroupinfo` / `tribeinfo` tables). Pins the standard `u16 cnt + (u32 key, u32 off)*` PABGH for node + spawn-data, the custom `u16 cnt + (u16 key, u32 off)*` PABGH for relation-group, and the conclusion that none of these tables localize through PALOC. Re-run on a new patch to confirm shapes still hold; on a structural drift the bridges' lossy parsers return empty handles rather than panic.
    - `_probe_store_mercenary_partprefab` + `_probe_itemkey_partprefab_linkage` + `_probe_partprefab_linkage_table_scan` + `_probe_partprefab_string_linkage` — four-phase RE behind the StoreKey / MercenaryKey / `_itemKey → _partPrefabKey` bridges. Pins the `(u16 key, u32 off)*` PABGH for storeinfo, the **new `(u8 key, u32 off)*`** 5-byte PABGH variant for mercenaryinfo, and the 3-table-join linkage (iteminfo → stringinfo → partprefabdyeslotinfo). Output to `out/store_mercenary_partprefab_probe/`.
    - `_probe_niche_bridge_candidates` — schema dump for the 12 niche-bridge candidate files (house, royalsupply, crafttool + group, triggerregion, gameplayvariable, globalgameevent + group, gameadvice + group, reserveslot, region, itemgroup). Each row's `[u16/u32 key][u32 name_len][name]` leading triple plus PABGH shape (standard `(u32, u32)` vs custom `(u16, u32)`). Output to `out/niche_bridges_probe/`.
    - `_probe_save_composite_types` — surveys every `(type_name, meta_size, meta_kind, decoded_kind)` triple in a live save, focusing on composite scalars (meta_kind 0/2 with size ∉ {1,2,4,8}). Drove the F32x3 / F32x4 / U32x4 typed-decode work in 2026-05-17; confirms Transform (40 B) is the only remaining size that falls through to `ScalarValue::Bytes`. Output to `out/composite_scalar_survey/summary.txt`.

   Invoke with `cargo test --lib --features c_abi <probe_name> -- --ignored --nocapture`.
6. ~~Bump the rolling-release tag in `build.yml`.~~ **No longer applicable** — `build.yml` and every `v1.0.<minor>.x` "Development Build" release were removed 2026-08-09 (no external consumers; see `../CLAUDE.md` → Development → Workflow). Nothing to publish on a new patch; `ci.yml` is the only workflow left.
7. **Snapshot the per-table key lists.** Run [`dump_gamedata_keys.py`](dump_gamedata_keys.py) to write the new `data\gamedata-keys-<ver>\<table>.txt` directory. This is the cross-version anchor snapshot for every non-iteminfo gamedata table (skill, mission, quest, stage, gimmick, character, faction, store, mercenary, the dye triple, the niche bridges, etc. — 30 tables, ~93 K keys for 1.08). Keep it committed alongside the new `data\keys-<ver>.txt`; future-patch diffs read it the same way `keys-1.07.txt` already serves iteminfo cross-version comparisons.

### Validated patches

| Patch | Items | iteminfo parse | Skill | Notes |
|---|---|---|---|---|
| 1.03 | — | byte-perfect | byte-perfect | baseline for skill cross-version drift table |
| 1.04 | — | byte-perfect | byte-perfect | |
| 1.05 | 6,236 | 100% ok | byte-perfect | first version where iteminfo was fully RE'd |
| 1.06 | 6,253 | 100% ok | live-roundtrip test green (no dedicated re-probe) | +17 items vs 1.05; no schema drift |
| 1.07 | 6,253 | 100% ok | live-roundtrip test green | identical item key list to 1.06; save format unchanged (v2 / flags 0x0080); slot100 + slot105 full-body roundtrip idempotent |
| **1.08** | **6,314** | **100% ok** | live-roundtrip test green | +61 items vs 1.07; **three schema drifts** in iteminfo (removed `extract_additional_drop_set_info: u32`; added `is_equip_quick_slot_visible: u8` between `is_housing_only` and `quick_slot_index`; added trailing `unk_post_summon_tag: u8` inside `DockingChildData`) — see `src/item_info/item.rs` header. Save format unchanged (v2 / flags 0x0080); slot100..slot107 + slot2 all full-body roundtrip. Cross-version-diff workflow that pinned the drifts is documented under "Investigation order" step 2 below. |
| **1.09** | **6,314** | **100% ok** (`leftover=0 fail=0 no_anchor=0`) | live-roundtrip test green | **content-only delta, NO schema drift.** `keys-1.09.txt` byte-identical to `keys-1.08.txt`. Gamedata-key snapshot diff vs 1.08: characterinfo +6 (7074), skill +5 (1980), knowledgeinfo +5 (6097), factionspawndatainfo +1 (123), gimmickinfo −8 (13073); all 25 other tables unchanged. Save format unchanged (v2 / flags 0x0080); slot105/slot107 (1.09 saves) parse + full-body decode + mutate→write→reload roundtrip clean; composite-scalar survey on slot105 matches 1.08 (Transform 40 B the only opaque fall-through). Full suite green after bumping the paver live-install pin 1.08→1.09 (only change needed). Verified on the live 1.09 install 2026-05-29. |
| **1.10** | **6,325** | **100% ok** (`leftover=0 fail=0 no_anchor=0`) | live-roundtrip test green | **+11 items vs 1.09. Two iteminfo schema drifts** (see `src/item_info/item.rs` header): removed `money_icon_path: StringInfoKey`; added `UnitData.unk_post_icon_path: u32` inside populated `MoneyTypeDefine`. RE'd vs the 1.09 sibling install (`out/iteminfo.1.09.pabgb`, kept for traceback) via `scripts/diff_109_110.py` + `verify_109_to_110.py` + `trace_size_mismatch.py`. **Also one save-body drift**: 1.10 widened the leading-pad on `ContentsMiscSaveData`'s ReflectObject lists 3→4 bytes; fixed by extending the object-list leading-pad scan 0..=3 → 0..=4 in `src/save/body/decoder.rs` (`_probe_contents_misc_drift` in `lib.rs` pinned it). Save format otherwise unchanged (v2 / flags 0x0080); slot107 (1.10 save) full-body decode + roundtrip clean. Gamedata-key snapshot diff vs 1.09 (`data/gamedata-keys-1.10/`): missioninfo +134 (6808), stageinfo +358 (51395), gimmickinfo +282 (13355), questinfo +65 (1084), knowledgeinfo +43 (6140), questgaugeinfo +43 (509), itemgroupinfo +14 (1521), gameadviceinfo +7 (470), characterinfo +8 (7082), skill +8 (1988), factionspawndatainfo +8 (131), partprefabdyeslotinfo +6 (1111), regioninfo +1 (1007), storeinfo +1 (293), factionnode −17 (1141). Soft test pins bumped (region/gameadvice/itemgroup counts, IS_EQUIP_QUICK_SLOT_VISIBLE 949→950, paver minor 9→10, quest_gauge prefix widened for `Mission_`-prefixed reconstruction gauge). Full suite green: 326 passed, 0 failed. Verified on the live 1.10 install 2026-06-05. **1.10.01 hotfix (verified 2026-06-06): equivalent, no parsing impact** — the 0008 container was repacked (localization dirs 0019+ also shuffled) but the extracted `iteminfo.pabgb` is **byte-identical** to 1.10 (same SHA256, 5,532,062 B); `paver` still reports minor=10. Anchor mode `ok=6,325 fail=0 leftover=0 no_anchor=0`, lossy mode 0 skipped, full `cargo test --lib --features c_abi,python` suite green again (326/0). No code or doc-schema changes needed; 1.10 support covers 1.10.01 verbatim. |
| **1.11** | **6,333** | **100% ok** (`leftover=0 fail=0 no_anchor=0`) | live-roundtrip test green | **+8 items vs 1.10. One iteminfo schema drift** (see `src/item_info/item.rs` header): a new boolean `u8` (`unk_post_apply_drop_stat_type`) inserted between `apply_drop_stat_type` and `drop_default_data`. RE'd via the lightweight tandem byte-walk vs the kept real-1.10 binary (`gamedata-bin/1.10/iteminfo.pabgb` in the local version archive, 5,532,062 B) — every common item gained exactly one byte at the `drop_default_data` boundary (net +1 on 6,321 / 6,325 common items; value reads 0/1 only, 4,861 one / 266 zero on the cleanly-isolatable subset). iteminfo size 5,532,062 → 5,543,339 B (+11,277). **No save-body drift**: all live slots (slot0/1/2, slot100–108) parse with `hmac_ok=true`, body decode `undecoded_bytes=0`, and body-stable write roundtrip (decode→encode→re-decode identical); save format unchanged (v2 / flags 0x0080). slot100 (old-format save) and slot102 (its 1.11 save-as) both clean. Gamedata-key snapshot diff vs 1.10 (`data/gamedata-keys-1.11/`): gameplayvariableinfo +8 (55), missioninfo +9 (6817), stageinfo +9 (51404), questinfo +1 (1085); all 26 other tables unchanged. Soft test pins bumped: paver live minor 10→11, gameplayvariableinfo count 47→55 (two sites). Full suite green: 329 passed, 0 failed. Verified on the live 1.11 install 2026-06-12. **Caveat:** the 1.10.01 hotfix install is no longer a real 1.10 (its 0008 container is byte-identical to the 1.11 D: install); use the kept `gamedata-bin/1.10/iteminfo.pabgb` baseline for any future 1.10→N cross-version diff. These per-version baselines are gitignored game content kept in a **portable local version archive** (`gamedata-bin/<ver>/`) — drive letter irrelevant; copy the folder to any machine that needs cross-version RE, then drop the baseline into `out/` the way the `diff_*.py` scripts expect. |
| **1.12** | **6,483** | **100% ok** (`leftover=0 fail=0 no_anchor=0`) | live-roundtrip test green | **+150 items vs 1.11. FOUR iteminfo schema drifts** (largest since 1.08 — see `src/item_info/item.rs` header): (1) `SubItem type_id==16` payload-free variant (15→16 on 4,496 items, both SubItem sites); (2) unconditional `unk_pre_max_endurance: u32` (=0) between `respawn_time_seconds` and `max_endurance`; (3) `unk_pre_gimmick_visual: u32` (=0) between `enchant_data_list` and `gimmick_visual_prefab_data_list`, **conditional** on `equip_type_info != 0 || item_type == 74` (gem) — fp=0 fn=0 across 4,496 clean items; (4) `enchant_data_list` gained a u32 (=0) **between** consecutive `EnchantData` elements (N elements → N-1 separators), modeled by the custom `EnchantDataList`. RE'd via `scripts/diff_111_112.py` (tandem byte-walk vs `gamedata-bin/1.11/iteminfo.pabgb`, 5,543,339 B) + per-boundary byte dumps. Drift #3 (first sibling-gated conditional field) drove a `=> <cond>` extension of the `py_binary_struct!` macro (read-gated `Option<T>`; see `src/binary/mod.rs`). iteminfo size 5,543,339 → 5,754,919 B (+211,580). Full `serialize_iteminfo` round-trip byte-identical on the live binary (in == out). **No save-body drift**: slot107 (live 1.12 save) `hmac_ok=true`, body decode `undecoded_bytes=0` (1107 blocks / 3098 fields), body-stable write round-trip clean; save format unchanged (v2 / flags 0x0080). Gamedata-key snapshot diff vs 1.11 (`data/gamedata-keys-1.12/`, 30 tables / 94,608 keys, all PABGH shapes still auto-detect): gimmickinfo +91 (13446), knowledgeinfo +72 (6212), storeinfo +53 (346), stageinfo +37 (51441), itemgroupinfo +20 (1541), characterinfo +7 (7089), skill +5 (1993), reserveslot +4 (31), crafttool(group)info +2 each, gameadviceinfo +2 (472), questinfo +2 (1087), gameplayvariableinfo +1 (56); **partprefabdyeslotinfo −143 (968), factionnode −22 (1119)**; all 15 other tables unchanged. **partprefabdyeslotinfo also drifted its per-slot *record* schema** (beyond the −143 row drop, which alone would not break parsing): 1.12 inserts a `u8 + u32` (uniformly `(0xFF, 0)`) between each slot's `mask` and `tail` CString (+5 B/slot), which broke the lossy parser (0 rows; the dye-editor + item-part-prefab bridges cascaded). Fixed in [`../src/part_prefab_dye_slot_info/mod.rs`](../src/part_prefab_dye_slot_info/mod.rs) by trying the 1.12 per-slot layout then falling back to the 1.07-1.11 layout per row — empirically disjoint (0 rows parse cleanly under both across the full 1.11 + 1.12 tables), so the fallback is unambiguous. RE'd via [`diff_dyeslot_111_112.py`](diff_dyeslot_111_112.py); test pins refreshed (3 of 6 known keys were among the removed rows). See [`../docs/dye-editor-scope.md`](../docs/dye-editor-scope.md). Portable archive refreshed: `gamedata-bin/1.12/` (266 files, 132,021,551 B). Verified on the live 1.12 install (paver minor=12) 2026-06-19. **1.12.01 hotfix (verified 2026-06-19): equivalent, no parsing impact** — paver reports `1 / 12 / 1 / 0x3d944dd7` (minor still 12); the extracted `iteminfo.pabgb` is **byte-identical** to 1.12 (same SHA256 `b895e70…`, 5,754,919 B), anchor mode `ok=6,483 leftover=0 fail=0 no_anchor=0`; all 30 gamedata-key tables byte-identical to `data/gamedata-keys-1.12/`; save format unchanged (slot107 `hmac_ok`, `undecoded_bytes=0`, v2 / flags 0x0080). No code or doc-schema changes needed; 1.12 support covers 1.12.01 verbatim. `keys.txt` unchanged (bugfix, no item add/remove). |
| **1.13** | **6,508** | **100% ok** (`leftover=0 fail=0 no_anchor=0`) | live-roundtrip test green | **+25 items vs 1.12 (+29 / −4 keys). Largest STRUCTURAL change yet — a relocation + merge, not just field inserts.** (1) `SubItem` gained payload-free `type_id==17` (both sites, 16→17). (2) `prefab_data_list` + `gimmick_visual_prefab_data_list` — which sat in the item **middle** since 1.05 — were **merged into a single `CArray<MergedPrefabVisualData>` relocated to the item END** (after `repair_data_list`); the merged element interleaves both source structs (`scale`, prefab/animation name lists, equip-slot + tribe-gender lists, 3-byte flag tail), merged count = old prefab + gimmick totals. `enchant_data_list` + the equip/gem-gated `unk_pre_gimmick_visual` **stay** in the middle (1.12 order). (3) A constant 2-byte item tail (`0xff,0x00` → `unk_tail: u16`) closes every item. iteminfo 5,754,919 → 5,938,891 B (+183,972). Full `serialize_iteminfo` round-trip byte-identical on the live binary (in==out, 6,508 items). **RE gotcha (documented so it isn't re-derived):** the bytes right after `drop_default_data` for equipment look like a "+14 gated head block" but are just `enchant_data_list` (large count for equip) + the unchanged gated `unk_pre_gimmick_visual`; only the two prefab/gimmick lists actually moved. RE'd via `scripts/diff_112_113.py` + `decode_tail_113.py` (fix head/middle, read the parser's per-item *leftover* to expose the relocated block byte-exactly; needs the `out/ref_112.json` 1.12 layout dump from `dump_ref_112.py`). **No save-body drift**: slot107 (live 1.13 save, saved today) `hmac_ok`, body decode `undecoded_bytes=0` (1107 blocks / 3097 fields), all 12 live saves full-body round-trip + idempotent; format unchanged v2 / flags 0x0080. Gamedata-key snapshot diff vs 1.12 (`data/gamedata-keys-1.13/`, 30 tables / 95,186 keys, all PABGH shapes still auto-detect): partprefabdyeslotinfo +570 (1538) **— also a per-slot record-schema refinement: the 1.12 `(0xFF,0)` 5-byte pad is actually `u8 marker + u32 extra_layer_count`; 1.13's new dyeable gear sets count=1, adding a second material/dye layer (`DyeExtraLayer`). Fixed in `src/part_prefab_dye_slot_info/mod.rs` (RE'd via `decode_dyeslot_113.py`); all 1,538 rows now parse (was 1,529). See [`../docs/dye-editor-scope.md`](../docs/dye-editor-scope.md)** — characterinfo +16 (7105), gimmickinfo +15 (13461), itemgroupinfo +9 (1550), knowledgeinfo +8 (6220), skill +6 (1999), questinfo +4 (1091), mercenaryinfo +1 (19), gameplayvariableinfo +1 (57), missioninfo +1 (6818), stageinfo +1 (51442); **storeinfo −53 (293), reserveslot −1 (30)**; all 17 other tables unchanged. Portable archive refreshed: `gamedata-bin/1.13/` (268 files, 132,831,315 B). Verified on the live 1.13 install (paver `1 / 13 / 0 / 0x536a2c0d`) 2026-07-04. |
| **1.14** | **6,508** | **100% ok** (`leftover=0 fail=0 no_anchor=0`) | live-roundtrip test green | **content-only over 1.13 — NO schema drift in any subsystem crimson-rs parses; zero parser-logic changes, only the version pin bumped.** iteminfo.pabgb is the same size (5,938,891 B) but **not** byte-identical to 1.13 (SHA256 `DE621624D0704BF5FE75E86F71219CE1BBDA6BF3F35AA44913E84AC7378778BD` vs 1.13.01's `D1BCF579…F02D`) → item field **value** changes only; full `serialize_iteminfo` round-trip byte-identical (6,508 items parsed, 0 skipped). **Item key set unchanged from 1.13** — all 6,508 keys resolve (`no_anchor=0`) and lossy total = 6,508, so `data/keys-1.14.txt` is byte-identical to `keys-1.13.txt`; no CE `keys.txt` re-dump needed. **No save-body drift**: slot107 (live 1.14 save, saved 2026-07-17) `hmac_ok`, body decode `undecoded_bytes=0` (1107 blocks / 3097 fields — same shape as 1.13), all 12 live saves full-body round-trip + idempotent; format unchanged v2 / flags 0x0080. Gamedata-key snapshot diff vs 1.13 (`data/gamedata-keys-1.14/`, 30 tables / 95,185 keys, all PABGH shapes still auto-detect): **knowledgeinfo −1 (6219)**; all 29 other tables byte-identical. Full `cargo test --lib --features c_abi` = 332 pass / 1 fail pre-bump, where the sole failure was the paver live-minor canary (`14 != PARSER_TARGET_GAMEDATA_MINOR 13`, by design); green after bumping the pin 13→14. Verified on the live 1.14 install (paver `1 / 14 / 0 / 0x597d42f8`) 2026-07-17. Portable archive refreshed: `gamedata-bin/1.14/` (268 files, 132,837,679 B; snapshotted 2026-07-24 from the still-live 1.14 install ahead of the 1.15 update, iteminfo SHA256 re-confirmed `DE621624…78BD` — same 268-file roster as 1.13, iteminfo/knowledgeinfo/stringinfo bytes differ). |
| **1.15** | **6,508** | **100% ok** (`leftover=0 fail=0 no_anchor=0`) | live-roundtrip test green | **content-only over 1.14 — NO schema drift in any subsystem crimson-rs parses; zero parser-logic changes, only the version pin bumped 14→15.** iteminfo.pabgb is the same size (5,938,891 B) but **not** byte-identical to 1.14 (SHA256 `C7AE554339D174D950ED001891052068014916C7FBEC5F3AEACB77FF3A2DD7CB` vs 1.14's `DE621624…78BD`) → item field **value** changes only; full `serialize_iteminfo` round-trip byte-identical (6,508 items parsed, 0 skipped). **Item key set unchanged from 1.14** — all 6,508 keys resolve (`no_anchor=0`), so `data/keys-1.15.txt` is byte-identical to `keys-1.14.txt`; no CE `keys.txt` re-dump needed. Gamedata-key snapshot diff vs 1.14 (`data/gamedata-keys-1.15/`, 30 tables / 95,185 keys, all PABGH shapes still auto-detect): **byte-identical — zero key changes in any table** (even lighter than 1.14, which had `knowledgeinfo −1` vs 1.13). Of the 268 extracted bin files, **249 are byte-identical to 1.14 and only 19 changed** (all value-only; skill/stringinfo/knowledgeinfo unchanged). **Save-body unaffected**: all save-body + slot107 live tests (all-items, positions, full-block decode, write→reparse round-trip) green; format unchanged. Full `cargo test --lib --features c_abi` = 332 pass / 1 fail pre-bump, sole failure the paver live-minor canary (`15 != PARSER_TARGET_GAMEDATA_MINOR 14`, by design); green after bumping the pin 14→15. Portable archive refreshed: `gamedata-bin/1.15/` (268 files, 132,838,769 B; iteminfo SHA256 re-confirmed `C7AE5543…DD7CB`). Verified on the live 1.15 install (paver major 1 / minor 15; canary printed `left: 15`) 2026-07-24. |
| **1.16** | **6,581** | **100% ok** (`leftover=0 fail=0 no_anchor=0`) | live-roundtrip green **after a parser fix** | **+73 items vs 1.15 (73 added, 0 removed). FOUR iteminfo schema drifts + the first SKILL drift since the parser was written.** iteminfo 5,938,891 → 6,145,386 B (+206,495); net per-item delta **+24 B** on 6,085 of 6,508 common items (−2 −1(cond) +10 +16). iteminfo SHA256 `491C00A805105C3633F7BB25E500E6C1074DC0B853D58739A4FE6DCBB445A40E`. Full `serialize_iteminfo` round-trip byte-identical (in == out, 6,581 items, 0 skipped). **The four iteminfo drifts** (see the "ItemInfo (1.16)" header in `src/item_info/item.rs`): (1) head-side `inventory_info: InventoryKey` **removed**; (2) `DockingChildData::unk_post_summon_tag: u8` (added in 1.08) **removed** — conditional, visible only on the 391 items with `docking_child_data.__tag__ != 0`, which partitions the table perfectly (fp=0 fn=0); (3) a **10 + 28×N byte block** (`u32 + u8 flag + CArray<UnkPreRespawnData> + u8`) inserted before `respawn_time_seconds`, **and `unk_pre_max_endurance` swapped to sit before `respawn_time_seconds`** — only 14 items have a non-empty list, so it costs a flat 10 B elsewhere, which is why it first read as a fixed 10-byte insert; the swap is what makes `respawn_time_seconds` decode sanely (0 / −1 / 604800 = 7 days) instead of −4294967296 / 0xffffffff; (4) `inventory_info` **reappears at the item END widened to `[u16; 9]`** (18 B), absorbing the 1.13-era constant `unk_tail` as slot 8 — slot 0 equals the old head-side value on all 6,054 cleanly-diffable items (zero exceptions), and slot 8 reads 6 instead of 0xFF on exactly the 59 `Trade_*_PackedInVehicle` items, the same 59 whose slot 7 reads 7. RE'd via [`diff_115_116.py`](diff_115_116.py) (clone of `diff_112_113.py`) + per-site decodes; the 1.15 sibling parser was rebuilt from `git archive HEAD` + `maturin build` + `pip install --target` when the in-tree parser could no longer read the 1.15 baseline. **SKILL drift**: a new `u8` (`PostBuff::unk_pre_damage_type`, reads 0 everywhere) between `is_use_child_pattern_description_buff_data` and `damage_type`; every entry gains exactly 1 B (delta +1 on 1,977 of 1,997 common keys). Before the fix **589 of 2,013 entries failed** — the new `_probe_skill_entry_failures` diagnostic (`cargo test --lib _probe_skill_entry_failures -- --ignored --nocapture`) is what showed this was systematic drift rather than one odd new skill; `try_parse_post_buff_end`'s flag-block size went 6 → 7. skill.pabgb 1,228,028 → 1,234,380 B, keys 1999 → 2013 (+16 / −2). **No save-body drift**: all 12 live slots `hmac_ok`, body decode `undecoded_bytes=0`, format unchanged v2 / flags 0x0080; slot107 is the live 1.16 save (written 2026-08-01). Gamedata-key snapshot diff vs 1.15 (`data/gamedata-keys-1.16/`, 30 tables / 96,076 keys, all PABGH shapes still auto-detect): gimmickinfo +228 (13689), stageinfo +153 (51595), knowledgeinfo +148 (6367), characterinfo +139 (7244), storeinfo +139 (432), itemgroupinfo +47 (1597), partprefabdyeslotinfo +16 (1554), skill +14 (2013), missioninfo +4 (6822), questinfo +4 (1095), mercenaryinfo +2 (21); **gameplayvariableinfo −1 (56), reserveslot −2 (28)**; all 17 other tables byte-identical. Extracted bin roster grew 268 → **270 files** (new: `activitypresetinfo.pabgb/.pabgh`, not a table crimson-rs parses); 107 of the 268 common files byte-identical. Soft test pins bumped: gameplayvariableinfo 57→56, itemgroupinfo 1550→1597, mercenaryinfo 19→21, reserveslot 30→28 (each in both the lossy and c_abi test), and `Pyeonjeon_Arrow` item_type 23→0 (a game-side enum remap — item_tier / quick_slot_index / flags unchanged). Full `cargo test --lib --features c_abi` = **333 passed / 0 failed**. Portable archive: `gamedata-bin/1.16/` (270 files, 138,531,192 B). Verified on the live 1.16 install (paver `1 / 16 / 0 / 0x8d1d6de1`) 2026-08-01. |
| **1.17** | **6,572** | **100% ok** (`leftover=0 fail=0 no_anchor=0`) | live-roundtrip green, `_probe_skill_entry_failures` = 2013/2013 ok | **content-only over 1.16 — NO schema drift in any subsystem; zero parser-logic changes, only version pins bumped.** −9 items (6,581 → 6,572, none added): keys **1004912–1004920**, the nine `Item_Set_{Weapon_Shield,Weapon_TwoHandWeapon,Weapon_RangeWeapon,Weapon_OneHandWeapon,Armor_Armor,Armor_Cloak,Armor_Boots,Armor_Gloves,Armor_Helm}_Tier0_Reminiscence` entries. iteminfo 6,145,386 → 6,139,734 B (SHA256 `F5D1BA508DC1A4225878DB070AE392F5872A1DA3B27620CA7621CBABF03F3535`); the −5,652 B delta is **exactly** the sum of those nine items' spans. Per-item proof of no layout drift: of the 6,572 common items, 6,435 byte-identical + 137 value-changed + **0 size-changed** (a pure value patch cannot mask a layout drift when no item's size moves). Full `serialize_iteminfo` round-trip byte-identical (in == out, 6,572 items, 0 skipped). **Skill untouched**: `skill.pabgb`/`.pabgh` are **byte-identical** to 1.16, so the parser is trivially green (2,013 entries, format `WithField58`). ⚠️ The first `_probe_skill_entry_failures` run aborted with `memory allocation of 17115381760 bytes failed` — that is the documented non-deterministic `TailSizeCache` hash-order artifact, **not** drift; the immediate re-run reported 2013/2013 ok. **No save-body drift**: slot107 is the live 1.17 save (written 2026-08-09); `test_save_body_decode_all_blocks` reports 1,107 blocks / 3,097 fields 100% decoded, `undecoded bytes: 0/5,204,773`, trailing_pad on 236 blocks — same shape as 1.13–1.16; all live-save c_abi roundtrips (inventory, socket, dye, deferred-redecode, mutate→write) green. Gamedata-key snapshot diff vs 1.16 (`data/gamedata-keys-1.17/`, 30 tables / 96,076 keys, all PABGH shapes still auto-detect): **gimmickinfo +1 (13690, new key 1012695), itemgroupinfo −1 (1596, removed key 18566)**; all 28 other tables key-identical. (The dumper writes LF while the committed 1.16 dumps check out as CRLF under `core.autocrlf=true`, so a raw file-hash diff flags all 30 — compare key *sets*, not hashes.) The removed itemgroupinfo key is not among the test `KNOWN` pins. Extracted bin roster unchanged at **270 files** (138,531,192 → 138,522,961 B); **248 of 270 byte-identical**, 22 changed — and `skill.*`/`stringinfo.*` are not among them. Artifact↔Challenge 1:1 invariant re-verified: 141 `Challenge_SealedArtifact_*` missions, 0 with >1 artifact. Soft test pins bumped: paver `PARSER_TARGET_GAMEDATA_MINOR` 16→17, itemgroupinfo 1597→1596 (both the lossy and the c_abi test). Full `cargo test --lib --features c_abi` = **333 passed / 0 failed** (330/3 pre-bump, the 3 being exactly those pins); strict clippy gate clean. Portable archive: `gamedata-bin/1.17/` (270 files, 138,522,961 B). Verified on the live 1.17 install (paver `1 / 17 / 0 / 0xd05e4c97`) 2026-08-09. |
| **1.18** | **6,573** | **100% ok** (`leftover=0 fail=0 no_anchor=0`) | live-roundtrip green, `_probe_skill_entry_failures` = 2027/2027 ok | **+1 item vs 1.17 (key 1005446 `Demian_Greyfur_Fabric_Cloak_II`, none removed). ONE iteminfo schema drift**, the smallest structural patch since 1.11: every `MergedPrefabVisualData` element gained a `u32` (`unk_pre_is_craft_material`) between `tribe_gender_list` and the 3-byte flag tail. iteminfo 6,139,734 → 6,190,316 B (+50,582); SHA256 `771FECB350BAA83BF77BB7BBD2756AEF8F7F47C96BB9A7F95E5539C7EB81C8D7`. The new field reads the **same constant on all 12,274 elements of all 6,573 items** — `73 e1 c5 ea` (LE u32 `0xeac5e173`), the "empty string" Jenkins sentinel already documented under the 1.10 `money_icon_path` removal — so it is very likely a string/prefab name hash shipping unset; typed as a bare `u32` until a populated value turns up. Unconditional, not sibling-gated: per-item insert count == that item's 1.17 merged-element count on 5,794 of the 5,797 fully-realigned items, and per-item size delta is exactly +4 × element count (+0 on 822 items, +4 on 198, +8 on 4,679, +12 on 750, +16 on 36, +20 on 44, +24 on 20). Full `serialize_iteminfo` round-trip byte-identical (in == out, 6,573 items). RE'd via [`diff_117_118.py`](diff_117_118.py) (clone of `diff_115_116.py`) in a single pass — one field, no follow-up decode script needed. **Two report signatures are walk artifacts, NOT drifts** — 1.18 reorders the `item_group_info_list` u16s (pure value churn that dominates the diff, and the reason 775 items abort mid-walk), which manufactures compensating ±1 B pairs: `look_detail_mission_info rm:1B` (93×) pairs with `item_group_info_list[2] rm:11B ins:12B` (54×) + `rm:9B ins:10B` (39×), and `enable_alert_system_to_ui ins:1B` (5×) pairs with `item_group_info_list[2] rm:6B ins:5B` (5×); both net zero per item. **Skill is content-only**: `skill.pabgb` 1,234,380 → 1,238,918 B (+4,538), keys 2,013 → 2,027 (+14, none removed), and the probe reports 2027/2027 ok with format still `WithField58`, so a *changed* skill hash is not by itself evidence of drift. **No save-body drift**: slot107 is the live 1.18 save (written 2026-08-15); `test_save_body_decode_all_blocks` reports 1,105 blocks / 3,321 fields 100% decoded, `undecoded bytes: 0/5,244,197`, trailing_pad on 236 blocks — same shape as 1.13–1.17 (block/field counts move with the *save*, not the format; `undecoded_bytes=0` is the invariant). All live-save c_abi roundtrips (inventory, socket, dye, deferred-redecode, mutate→write) green. Gamedata-key snapshot diff vs 1.17 (`data/gamedata-keys-1.18/`, 30 tables / 96,197 keys, all PABGH shapes still auto-detect): partprefabdyeslotinfo +65 (1619), gimmickinfo +35 (13725, +40/−5), skill +14 (2027), knowledgeinfo +10 (6377), houseinfo +8 (12), storeinfo +5 (437), itemgroupinfo +1 (1597), triggerregioninfo +1 (13); **characterinfo −18 (7226)**; all 21 other tables key-identical. Extracted bin roster **270 → 268 files** — `zoneinfo.pabgb`/`.pabgh` were dropped, and nothing in crimson-rs references zoneinfo, so there is no parsing impact; 142 of the 268 common files byte-identical, 126 changed. Soft test pins bumped: paver `PARSER_TARGET_GAMEDATA_MINOR` 17→18, itemgroupinfo 1596→1597, houseinfo 4→12, triggerregioninfo 12→13 (each in both the lossy and the c_abi test), and `IS_EQUIP_QUICK_SLOT_VISIBLE` 1005→1006 (the new cloak is equipment). One pin was **re-shaped rather than re-numbered**: `part_prefab_dye_slot_info_lossy_live` asserted `slot_count==1` on >1/4 of rows, and 1.18's 65 new rows pushed that to 399/1,619 = 24.6%. All 1,619 rows parse (== the pabgh key count) and every `KNOWN` prefab name + slot_count still matches, and the histogram is textbook right-skewed (399/323/246/185/112/79/53/47/33/21/… peak 36), so this is content, not the record-schema drift the check guards against (1.12 broke it to 0 rows, 1.13 to 1,529/1,538). Replaced the fixed fraction with "`slot_count==1` must be the modal bucket" and made the test print the histogram. Full `cargo test --lib --features c_abi` = **333 passed / 0 failed** (327/6 pre-bump, the 6 being exactly those pins); strict clippy gate clean. ⚠️ Run the suite with `-- --test-threads=4`: at the default thread count two consecutive runs ballooned past 13 GB and took the whole test process down with `STATUS_STACK_BUFFER_OVERRUN` — the same non-deterministic `TailSizeCache` allocation artifact documented for the skill probe, but here it aborts *every* test, not one. Portable archive: `gamedata-bin/1.18/` (268 files, 139,120,287 B). Verified on the live 1.18 install (paver `1 / 18 / 0 / 0x28577c0f`) 2026-08-15. |
| **2.00** | **6,810** | **100% ok** (`leftover=0 fail=0 no_anchor=0`) | live-roundtrip green, `_probe_skill_entry_failures` = 2046/2046 ok | **+237 items vs 1.18 (237 added, 0 removed). The first *major* version bump — paver `2 / 0 / 0 / 0xb241d214`, i.e. major 1 → 2 and minor 18 → **0**.** That reset is the patch's one ABI consequence: `PARSER_TARGET_GAMEDATA_MINOR` is now `0`, which a minor-only compatibility check cannot tell apart from a hypothetical 1.00, so a sibling `PARSER_TARGET_GAMEDATA_MAJOR = 2` was added alongside it and exposed as the new `crimson_parser_target_gamedata_major()` C ABI (additive — the existing minor + compatible-minors bridges are unchanged, and `COMPATIBLE_GAMEDATA_MINORS` entries are now read as minors *within* that major). **TWO iteminfo schema drifts** (see the "ItemInfo (2.00)" header in `src/item_info/item.rs`): (1) `SubItem` gained payload-free `type_id == 18` — the same renumbering 1.12 (16) and 1.13 (17) did; in 2.00 *every* site that read 17 reads 18 (item-level `default_sub_item` on all 6,810 items, `drop_default_data.default_sub_item` on 6,806 — the other 4 read tag 0), and no item still reads 14..=17. (2) A new always-zero `u32` (`unk_pre_max_endurance_a`) inserted **directly ahead of** the 1.12-era `unk_pre_max_endurance`, so the block before `respawn_time_seconds` now carries two u32s. Unconditional: 6,513 of the 6,573 carried-over items are exactly +4 B, and on 1,500 sampled carry-overs `old[max_endurance..] == new[max_endurance + 4..]` byte-for-byte (1500/1500). ⚠️ **RE gotcha worth internalising — the byte diff cannot place this field.** It lands inside an all-zero run, so inserting it before `unk_pre_max_endurance`, between the two, or after `respawn_time_seconds` all produce identical bytes and all round-trip byte-perfectly. What settles it is the *value-sanity* argument from the 1.16 swap: only with the new u32 first do both neighbours keep their known distributions — `unk_pre_max_endurance` still reads `0x01000000` on exactly the 59 `Trade_*_PackedInVehicle` items (0 on the other 6,751) and `respawn_time_seconds` still reads 0 (6,053) / −1 (755) / 604800 (2 — `Item_Egg_KuKu`, `Item_Egg_Wyvern`, i.e. 7 days). The wrong placements drag `0x01000000` into the new field and turn the eggs' 604800 into `604800 << 32` and −1 into `−4294967296` — the exact nonsense signature 1.16 documented. **Check value distributions, not just the round-trip.** iteminfo 6,190,316 → 6,446,719 B (+256,403); SHA256 `51F87FB41046C1D8DE9F84DE6F11E51BA2A837205F121FA5825552C2E6948746`. Full `serialize_iteminfo` round-trip byte-identical (in == out, 6,810 items, 0 skipped). The 1.18 `MergedPrefabVisualData.unk_pre_is_craft_material` sentinel is unchanged — still the constant `0xeac5e173`, now on all 12,550 elements. **Skill is content-only**: `skill.pabgb` 1,238,918 → 1,261,935 B (+23,017), keys 2,027 → 2,046 (+19, none removed), probe 2046/2046 ok, format still `WithField58`. **No save-body drift**: `test_save_body_decode_all_blocks` reports 1,103 blocks / 3,315 fields 100% decoded, `undecoded bytes: 0/5,229,306`, trailing_pad on 236 blocks — same shape as 1.13–1.18; all live-save c_abi roundtrips green. Gamedata-key snapshot diff vs 1.18 (`data/gamedata-keys-2.00/`, 30 tables / 96,997 keys, all PABGH shapes still auto-detect): knowledgeinfo +326 (6703), stageinfo +265 (51860), gimmickinfo +112 (13837), missioninfo +37 (6859), characterinfo +25 (7251), skill +19 (2046), gameadviceinfo +6 (478), gameplayvariableinfo +3 (59), globalgameevent +3 (191), questgaugeinfo +2 (511), questinfo +2 (1097), partprefabdyeslotinfo +1 (1620); **storeinfo −1 (436)**; all 17 other tables key-identical. The globalgameevent delta is one substitution, not growth: 2.00 **removed `RoyalSupply` (0x424a) and added four per-faction rows** `RoyalSupply_{Her,Dem,Del,Var}` (0x4308–0x430b), all still group 0x4241 with `paloc_key = 0`. Extracted bin roster **268 → 269 files** — `levelgimmicksceneobjectinfo_misc.base.pabgm` is new while `levelgimmicksceneobjectinfo.pabgb` shrank 3,410,666 → 21,376 B, i.e. its payload moved into that new `.pabgm` container; crimson-rs parses neither, so there is no impact. 143 of the 268 common files byte-identical, 125 changed — the largest churn in the archive. Soft test pins bumped: gameadviceinfo 472→478, gameplayvariableinfo 56→59, globalgameevent 188→191 (each in both the lossy and the c_abi test), `IS_EQUIP_QUICK_SLOT_VISIBLE` 1006→1008, and the `KNOWN_BODY` `RoyalSupply` triple replaced one-for-four. Full `cargo test --lib --features c_abi` = **334 passed / 0 failed** (314/19 pre-fix: 10 iteminfo-drift, 8 stale row counts, 1 paver canary); strict clippy gate clean. Portable archive: `gamedata-bin/2.00/` (269 files, 141,634,226 B). Verified on the live 2.00 install 2026-08-26. |
| **2.01** | **6,813** | **100% ok** (`leftover=0 fail=0 no_anchor=0`) | live-roundtrip green, `_probe_skill_entry_failures` = 2061/2061 ok | **A container reshuffle with ONE schema drift, and the drift is not in iteminfo.** Pearl Abyss renamed the gamedata directory and every extension in it — `0008/gamedata/binary__/client/bin/<t>.pabgb`/`.pabgh` → `0008/gamedata/binarystaticinfo__/bin/<t>.staticinfobody`/`.staticinfoheader` (the misc `.pabgm` moved to `bin/misc` as `.staticinfomisc`; the 13,908 gimmick-chart files became `gamedata/binarygimmickchart__/bin/*.binarygimmick`) — and split each language's single `localizationstring_<lang>.paloc` into 39 per-namespace files under `gamedata/stringtable/binary__/<lang>/`. **Contents were untouched**: PALOC has no header, so re-serialising the concatenated namespace entry lists reproduces the pre-2.01 blob, and `(key << 32) \| group` string_keys are unchanged. Nothing in the library hardcoded a path (all 189 `"gamedata/binary__/client/bin"` literals were `#[cfg(test)]` — confirmed by `strings crimson_rs.pyd` finding zero), so the fix is two resolvers: [`gamedata_layout.py`](gamedata_layout.py) and `src/binary/gamedata_layout.rs`, both newest-layout-first with fallback. **The one real drift**: `partprefabdyeslotinfo`'s per-slot `mask` widened `[u8; 3]` → `[u8; 12]`, in the slot *and* the 1.13-era extra layer — before the fix the table parsed 0 of 1,626 rows. **Pinned by tandem walk against `gamedata-bin/2.00/`**, not by distribution: reading 2.00 at `mask_len = 3` and 2.01 at `12`, 1,613 of the 1,620 carried-over rows have every non-mask field byte-identical, and the 7 that don't differ only by ordinary content (3 changed `slot_count`, 4 changed a material name / `mat_indices`). **Contents were re-encoded, not extended** — in 5,572 of 6,555 comparable slot pairs the 2.00 three bytes appear nowhere as a contiguous window in the 2.01 twelve, so no sub-slice is the old field. The 12 read as four groups of three (exactly one group non-zero on 4,276 of 6,585 slots, two on 1,254, three on 72, none on 983, never four; summed the mask equals the non-empty-material-name count on 62.2% of slots vs 39.4% for `mask[0..3]`). **Lesson**: the kept per-version `gamedata-bin/` archive answers "where did the bytes go" outright — reach for it before arguing from value distributions. The 3-byte `..._lookup_slot_mask` C ABI is now a partial read (zeros when a slot's active group isn't the first), so `..._lookup_slot_mask_full` / `..._lookup_slot_extra_layer_mask_full` were added alongside it — sized-buffer `(buf, buf_len, required)`, so a future widening returns `BUFFER_TOO_SMALL` rather than truncating. iteminfo: +3 items (6,810 → 6,813; keys 1005600 / 1005601 / 1005852, none removed; 1000319 moved position), 6,446,719 → 6,450,232 B (SHA256 `e646e4a0…`), **zero layout drift**, byte-perfect `serialize_iteminfo` roundtrip. Skill: 2,046 → 2,061 entries, format still `WithField58`, header + body both byte-perfect. Save body clean. Gamedata-key set 30 tables / 97,119 keys, 20 tables byte-identical to 2.00; the 10 that moved: gimmickinfo +69, skill +15, knowledgeinfo +11, houseinfo +10 (12 → 22), partprefabdyeslotinfo +6, missioninfo +5, gameplayvariableinfo +3, itemgroupinfo +3, stageinfo +1, characterinfo −1. Extracted bin roster still 269 files. 2.01 also ran a **localization copy pass**: `Mission_Intro_Tutorial_I` "Unfamiliar Lands" → "Unfamiliar Land" (the *stage* `Intro_Tutorial_I` still reads "Unfamiliar Lands") and `Mission_MeetAlustain_Alustain_Wisdom` "Mysterious Man" → "A Mysterious Beggar" — both are keys of the `main_quest_chapter` curated table, so retitles silently break that bridge and need checking every patch. `cargo test --lib --features c_abi` = 334 pass / 0 fail (`-- --test-threads=4`, per the 1.18 note). |

## Sanity-check on a fresh checkout / new patch

```powershell
python scripts\export_for_ce.py
```

Expect `parser status: ok=<N>  leftover=0  fail=0  no_anchor=0` where `N` matches the line count of `data\keys.txt` (the in-game item-key dump). For 1.06 / 1.07 the expected line is `ok=6,253  leftover=0  fail=0  no_anchor=0`. A non-zero `no_anchor` means `keys.txt` has entries past the real array end — see the "keys.txt structure" section below.

For a finer-grained per-cluster view if anything breaks:

```powershell
python scripts\anchor_diff.py --keys data\keys.txt --pabgb out\iteminfo.pabgb `
    --baseline out\baselines\1.04\items.jsonl --out out\anchors.json
python scripts\analyze_per_item.py --anchors out\anchors.json --pabgb out\iteminfo.pabgb
```

## Investigation order if a future patch breaks parsing

1. **Sanity-check the anchor scanner first.** [`build_items_jsonl.py`](build_items_jsonl.py) `looks_like_item_start` validates `[u32 key, u32 slen, slen identifier-bytes, u8 zero]`. If the new patch introduces longer names (`slen > 128`) or new identifier bytes, the scanner mis-anchors and downstream looks like a schema bug. Lesson from the 1.05 RE — see [`../docs/archive/1.05-parser-history.md`](../docs/archive/1.05-parser-history.md) Phase 3.
2. **Then check for genuine schema drift.** Two workflows; pick whichever the situation calls for:
    - **Lightweight (no sibling parser) — used for the 1.07 → 1.08 RE.** Keep one copy of the *previous* version's extracted `iteminfo.pabgb` next to the new one (e.g. snapshot `out/iteminfo.pabgb` to `out/iteminfo.<old>.pabgb` before refreshing the live install). The current parser fails on the new binary; the OLD binary still parses, so `crimson_rs.parse_iteminfo_tracked` gives you the old per-item span/offset map. Then for every key in `keys.txt` that's also in the old binary, run a precise per-byte tandem walk between the two item chunks: at each mismatch, brute-force shifts in `[1..8]` bytes on each side, accept the shift that yields ≥30 consecutive matching bytes downstream, and record `(old_offset, new_offset, removed_bytes_hex, inserted_bytes_hex)`. The detected drift events identify exactly which fields were added/removed; cross-checking against three or more items (one minimal, one with populated optionals like `docking_child_data`, one with stat arrays) immediately exposes conditional schema branches. Final validation: reconstruct synthetic new-version bytes by applying the detected removals/insertions to the old item bytes and assert byte-equality with the real new bytes. This single check is what turned the 1.08 RE from "three failure clusters in the parser" into "three confirmed schema changes" without ever building a 1.07 sibling parser.
    - **Sibling-parser (for when the structural change is too large to byte-diff).** Set up the historical parser as a sibling install (recipe in [`../docs/historical-parser-setup.md`](../docs/historical-parser-setup.md)).

      **Lightest route, used for the 1.16 RE** — needed as soon as you edit the in-tree parser, because it can then no longer read the *previous* version's binary, which kills `parse_iteminfo_tracked`-based field maps on the old side:

      ```powershell
      git archive HEAD src Cargo.toml Cargo.lock pyproject.toml python .cargo | tar -x -C <tmp>
      cd <tmp>; maturin build --release -o dist
      pip install --target <tmp>\pkg --no-deps --force-reinstall <tmp>\dist\*.whl
      ```

      then `sys.path.insert(0, r"<tmp>\pkg")` before `import crimson_rs` (and `del sys.modules["crimson_rs"]` between the two imports if you need both parsers in one process). **Don't reach for `git worktree add`** — it fails on this repo with `external filter 'git-crypt smudge' failed` on `tests/fixtures/saves/`. `git archive` with an explicit path list sidesteps the encrypted fixtures entirely.
 Use the cross-version diff templates in [`archive/`](archive/) — copy, rename to the new version pair, adapt path constants. This was the workflow for the larger 1.04 → 1.05 RE; the lightweight workflow above is enough for incremental patches.
3. **Don't add new schema fields by eyeballing diff output.** Difflib is unreliable when fields are zero-padded. Use `align_<old>_<new>.py`-style cumulative-shift analysis OR the precise tandem walk above to pinpoint the exact field where bytes were inserted.
4. **For conditionally-present new fields, identify the discriminator before patching.** If only a subset of items show a drift (e.g. 1.08's `DockingChildData::unk_post_summon_tag` only materialised for the 385 items with `docking_child_data.tag = 1`), cross-tab the new field's presence against every flag/COptional/CArray-count in the parser-tracked spans of the OLD version. The discriminator is whichever column gives a perfect partition — that's the schema's conditional clause.

## Layout invariants

- Every item begins with `u32 key` then `u32 string_key.len` then `len` bytes of identifier content. **There is no trailing NUL on `CString`** in this codebase — what looked like a NUL in earlier docs is actually the next field's first byte (`is_blocked: u8 = 0` for almost every item). The anchor scanner exploits that incidental zero as a cheap discriminator.
- Item keys are bounded comfortably below 2^24 (a ~6-digit decimal). The `(key >> 24) == 0` check in `scan_next_item_start` is solid.
- `string_key` length in 1.05 / 1.06 ranges 2..71 bytes. The scanner uses `2..=128` for headroom. Bytes are ASCII alphanumeric / `_` / ` ` *or* UTF-8 high bytes (1.05 introduced Roman numerals Ⅲ/Ⅳ/Ⅵ in some Goblin_Merchant_* names; 1.06 added 17 items without breaking the byte set).
- `data/keys.txt` is the ground truth for "is this key actually loaded into the game." Per-version totals: 6,236 keys in 1.05 (vs 6,389 paloc 0x70 entries — paloc carries 153 extras); 6,253 keys in 1.06 (vs 6,405 paloc 0x70 entries — 152 paloc extras).
- The same key value can appear multiple times in the binary: once as the item's own `key`, again as embedded `ItemKey`-typed fields in other items (`inventory_info`, `equip_type_info`, `convert_item_info_by_drop_npc`, …), and once more as the `(key << 32) | 0x70` paloc lookup index serialized as a numeric string in `item_name.default` for the 71 dev items. The scanner's `[key, slen, identifier-content, zero]` shape check is what disambiguates real anchors from those embeds.

## keys.txt structure (what the CE Lua dumper writes)

The in-game itemKey array lives at runtime as a packed `[u32 key][u32 unk]` table. **`unk` is the byte offset of that item inside `iteminfo.pabgb`** — discovered in 1.06 by dumping the array region and watching `unk` increase monotonically by ~600 B/slot, with the final slot's `unk` matching the anchor offset of the last item in the binary exactly. Practical consequences:

- The dumper Lua (`dump_item_keys.CEA` in `Mydev-Cheat-Engine-Tables`) now **auto-terminates on `unk` monotonicity break**. Stop reasons it can emit: `unk non-monotonic at idx=N` (the array's real end), `key sentinel at idx=N (key=0xFFFFFFFF or 0)`, or `page fault`. No more manual trimming — what lands in `keys.txt` is exactly the live array.
- `_find_anchors` in `export_for_ce.py` returns `list[int | None]` and the caller emits a `no_anchor` fallback record for any key whose item is missing from `iteminfo.pabgb`. This is the safety net for an over-trimmed (or under-trimmed) `keys.txt`; on a clean dump `no_anchor=0`.
- Bonus cross-check: the offset returned by the anchor scanner for each item should match the `unk` field for that key in the live-memory array. If a future patch shows mismatch, the anchor scanner is mis-anchoring.

## Don't

- Don't reintroduce the `new_icon_path` / `ammo_mid_block` / `ItemInfoTail` (3u8 + sentinel) variant-tail model. It coincidentally round-trips on a subset of items but is fundamentally wrong; see [`../docs/archive/1.05-parser-history.md`](../docs/archive/1.05-parser-history.md) Phase 1.
- Don't tighten the anchor scanner's `slen` bound or `is_ident_byte` set without checking real items first — Pearl Abyss has used UTF-8 (Ⅲ/Ⅳ/Ⅵ Roman numerals) and 70+-byte names in 1.05.
- Don't remove `parse_iteminfo_lossy` or the anchor pipeline. They're the user-facing safety net for any future patch that introduces unexpected schema drift.
- Don't commit anything under `out/`, `references/samples/`, `out/baselines/`, or `.crimson_rs_*/`. Those contain extracted Pearl Abyss content and locally-built historical wheels. `.gitignore` already excludes them; double-check after edits.

## Where to find things

- Active diagnostic / production scripts → this directory ([`README.md`](README.md) has the index)
- 1.04 → 1.05 cross-version diff templates → [`archive/`](archive/)
- Skill cross-version drift probe (run on new game patches) → [`archive/probe_skill_versions.py`](archive/probe_skill_versions.py)
- Full iteminfo RE history → [`../docs/archive/1.05-parser-history.md`](../docs/archive/1.05-parser-history.md)
- Historical parser setup → [`../docs/historical-parser-setup.md`](../docs/historical-parser-setup.md)
- 71 dev/QA items investigation → [`../docs/paloc-71-dev-items.md`](../docs/paloc-71-dev-items.md)
- Status of downstream Python bindings (all done as of 2026-05) → [`../docs/archive/downstream-api-gaps.md`](../docs/archive/downstream-api-gaps.md)
